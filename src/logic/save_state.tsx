// import { create } from "zustand";

// type State = {
//   progressPercentage: number;
// };

// type Action = {
//   update: (newProgress: number) => void;
// };

// export const useProgressStore = create<State & Action>((set) => ({
//   progressPercentage: 0,
//   update: (newProgress: number) => set({ progressPercentage: newProgress }),
// }));
